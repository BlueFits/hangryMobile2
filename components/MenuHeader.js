import React from "react";
import { View, StyleSheet } from "react-native";

//Controllers
import { DefaultText, HeaderText, SmallText } from "../controllers/TextController";

const MenuHeader = () => {
    return (
        <View>
            <DefaultText>Halou</DefaultText>
        </View>
    );
};

export default MenuHeader;